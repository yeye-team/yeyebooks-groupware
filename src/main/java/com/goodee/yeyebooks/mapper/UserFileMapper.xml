<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goodee.yeyebooks.mapper.UserFileMapper">
	<select id="selectUserFile">
		SELECT user_file_no userFileNo, (select code_nm from common_code where group_code = 'U001' and CODE = file_cat_cd) fileCategory, save_filename saveFilename, path
		FROM user_file
		WHERE user_id = #{userId}
	</select>
</mapper>